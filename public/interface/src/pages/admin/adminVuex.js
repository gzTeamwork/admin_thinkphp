//  a vuex store live template
const state = {
  adminUser: [],
  isAdmin: false,
  isLogin: false
};

const getters = {};

const actions = {};

const mutations = {};

export default {
  state: state, getters: getters, actions: actions, mutations: mutations
}

